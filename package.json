{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "migrate": "npx sequelize-cli --prefix backend db:migrate",
    "seed:all": "npx sequelize-cli --prefix backend db:seed:all",
    "seed:undo:all": "npx sequelize-cli --prefix backend db:seed:undo:all",
    "migrate:undo:all": "npx sequelize-cli --prefix backend db:migrate:undo:all"
  }
}
